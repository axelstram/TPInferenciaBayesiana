# Difference Between Two Rates
model{
   # Observed Counts
   k1 ~ dbin(theta,n1)
   k2 ~ dbin(theta,n2)
   k3 ~ dbin(theta_prime, n3)
   # Prior on Rates
   theta ~ dbeta(1,1)
   theta_prime ~ dbeta(1,1)
}
